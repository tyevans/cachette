# A restatement that no citation reaches

The stage costs below are all worked out from the shape of the pass. No
measurement exists on the target platform, so a reader may not take any of
them for a figure that a run produced.

The sentence above wraps across a line break, so a line based search finds
nothing. The check joins the paragraph before it matches.
