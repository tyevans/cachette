# Deliberately broken documents

These documents fail the register prose check on purpose. The probe recipe
scans this directory and fails when the check passes over it.

Do not repair a document here. Each one holds one defect, and the file name
says which.
