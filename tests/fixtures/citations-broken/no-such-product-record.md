This file is deliberately broken.

It cites PRD-9999, which no product record and no registry row has. The
check must fail on it. Continuous integration runs the check against this
directory and fails when the check passes.
