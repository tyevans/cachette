# Two orientations that disagree

The probe recipe checks this directory and fails when the check passes over
it. The two files below differ in one sentence, which is the shape the check
exists to catch.

Do not repair either file.
