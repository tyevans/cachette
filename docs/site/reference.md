# Reference

This page is generated. It holds the public interface of the compiled extension
module of the Python control plane. It holds no written interface prose.

The prose of each member below comes from the Rust doc comment that declares
it. The build imports the compiled module and reads the docstring that the
import creates. A contributor who changes what this page says changes that doc
comment, and never this file.

::: cachette._core
