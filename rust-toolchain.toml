# ADR-0008 names aarch64-unknown-linux-gnu as the primary target.
# The other targets are development targets only.
[toolchain]
channel = "1.91.1"
components = ["rustfmt", "clippy"]
targets = ["aarch64-unknown-linux-gnu"]
profile = "minimal"
